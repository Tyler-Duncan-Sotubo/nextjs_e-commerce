import React, { FC } from "react";
import UploadForm from "../../components/Admin/UploadForm";

const Upload: FC = () => {
  return (
    <>
      <UploadForm />
    </>
  );
};

export default Upload;
